@page
@model RegisterConfirmationModel
@{
    ViewData["Title"] = "Подтверждение регистрации";
}

<h1>@ViewData["Title"]</h1>
@{
    if (Model.DisplayConfirmAccountLink)
    {
        <p>
            Здесь нет реальной отправки почты. Как настроить — см. <a
                href="https://aka.ms/aspaccountconf">документацию</a>.
            Обычно вы бы получили письмо: <a id="confirm-link"
                                             href="@Model.EmailConfirmationUrl">Подтвердить аккаунт</a>
        </p>
    }
    else
    {
        <div class="alert alert-info"
             role="alert">
            <h4 class="alert-heading">Регистрация почти завершена!</h4>
            <p>
                Мы отправили письмо с кодом подтверждения на адрес <strong>@Model.Email</strong>.
            </p>
            <hr>
            <p class="mb-0">
                Проверьте свою почту и введите полученный код для завершения регистрации.
                Если письмо не пришло, проверьте папку "Спам".
            </p>
        </div>

        <div class="text-center mt-4">
            <a asp-page="./ConfirmEmailCode"
               asp-route-email="@Model.Email"
               asp-route-returnUrl="@ViewData["ReturnUrl"]"
               class="btn btn-primary btn-lg">
                Ввести код подтверждения
            </a>
        </div>
    }
}
