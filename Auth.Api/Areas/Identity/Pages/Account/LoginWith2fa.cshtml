@page
@model LoginWith2faModel
@{
    ViewData["Title"] = "Двухфакторная аутентификация";
}

<h1>@ViewData["Title"]</h1>
<hr />
<p>Ваш вход защищён приложением-аутентификатором. Введите код ниже.</p>
<div class="row">
    <div class="col-md-4">
        <form method="post"
              asp-route-returnUrl="@Model.ReturnUrl">
            <input asp-for="RememberMe"
                   type="hidden" />
            <div asp-validation-summary="ModelOnly"
                 class="text-danger"
                 role="alert"></div>
            <div class="form-floating mb-3">
                <input asp-for="Input.TwoFactorCode"
                       class="form-control"
                       autocomplete="off" />
                <label asp-for="Input.TwoFactorCode"
                       class="form-label"></label>
                <span asp-validation-for="Input.TwoFactorCode"
                      class="text-danger"></span>
            </div>
            <div class="checkbox mb-3">
                <label asp-for="Input.RememberMachine"
                       class="form-label">
                    <input asp-for="Input.RememberMachine" />
                    Запомнить этот браузер
                </label>
            </div>
            <div>
                <button type="submit"
                        class="w-100 btn btn-lg btn-primary">Войти
                </button>
            </div>
        </form>
    </div>
</div>
<p>
    Нет доступа к приложению-аутентификатору? 
    <a id="recovery-code-login"
       asp-page="./LoginWithRecoveryCode"
       asp-route-returnUrl="@Model.ReturnUrl">Войти по резервному коду</a>.
</p>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
