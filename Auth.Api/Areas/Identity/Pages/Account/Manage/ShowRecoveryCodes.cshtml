@page
@model ShowRecoveryCodesModel
@{
    ViewData["Title"] = "Коды восстановления";
    ViewData["ActivePage"] = "TwoFactorAuthentication";
}

<partial name="_StatusMessage"
         for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<div class="alert alert-warning"
     role="alert">
    <p>
        <strong>Сохраните эти коды в надёжном месте.</strong>
    </p>
    <p>
        Если вы потеряете устройство и не сохраните коды, доступ к аккаунту будет утрачен.
    </p>
</div>
<div class="row">
    <div class="col-md-12">
        @for (int row = 0; row < Model.RecoveryCodes.Length; row += 2)
        {
            <code class="recovery-code">@Model.RecoveryCodes[row]</code>
            <text>&nbsp;</text>
            <code class="recovery-code">@Model.RecoveryCodes[row + 1]</code>
            <br />
        }
    </div>
</div>
